enum ServerRoutes {
  USERS = 'users',
  POSTS = 'posts',
  COMMENTS = 'comments',
  AUTH = 'auth',
  FILES = 'files',
  TRIP_PLAN = 'trip-plan',
  TRIPS = 'trips',
  DESTINATIONS = 'destinations',
  JOIN_REQUESTS = 'join-requests',
  ALERTS = 'alerts',
}

export {ServerRoutes};
