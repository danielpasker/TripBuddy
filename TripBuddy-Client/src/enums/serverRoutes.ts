enum ServerRoutes {
  USERS = 'users',
  POSTS = 'posts',
  COMMENTS = 'comments',
  AUTH = 'auth',
  FILES = 'files',
  TRIP_PLAN = 'trip-plan',
  DESTINATIONS = 'destinations',
}

export {ServerRoutes};
