enum ClientRoutes {
  HOME = '/home',
  POST = '/post',
  COMMENTS = '/comments',
  TRIPS = '/trips',
  NEW_TRIP = `${TRIPS}/new`,
  JOIN_TRIP = `${TRIPS}/join`,
  PROFILE = '/profile',
  ALERTS = 'alerts',
}

export {ClientRoutes};
